import { ApiPlayground } from "../../components/ApiPlayground";

# Providers

## `GET /v1/providers`

List storage providers.

**Query parameters**

| Param | Type | Notes |
|---|---|---|
| `network` | string | Required |
| `status` | `active \| waitlisted \| all` | Default `all` |
| `az` | string | Filter by availability zone |
| `limit` | number | Default 50 |
| `offset` | number | Default 0 |

**Response**

```json
{ "providers": [], "count": 0, "total": 0 }
```

---

## `GET /v1/providers/:address`

Single provider detail, including reputation and PG assignment.

**Query parameters**

| Param | Type | Notes |
|---|---|---|
| `network` | string | Required |

**Response**

```json
{
  "address": "0x...",
  "reputationScore": 0,
  "uptimePct30d": 0,
  "designatedPgs": [],
  "stake": "0",
  "epochHistory": []
}
```

---

## `GET /v1/providers/leaderboard`

Ranked provider leaderboard.

**Query parameters**

| Param | Type | Notes |
|---|---|---|
| `network` | string | Required |
| `sort` | `score \| uptime \| stake` | Default `score` |
| `limit` | number | Default 20 |

**Response**

```json
{ "leaderboard": [{ "rank": 1, "address": "0x...", "score": 0 }] }
```

---

## `GET /v1/providers/:address/history`

Health history for a single provider.

**Query parameters**

| Param | Type | Notes |
|---|---|---|
| `network` | string | Required |
| `from` | ISO timestamp | |
| `to` | ISO timestamp | |
| `resolution` | string | Default `1h` |

**Response**

```json
{ "history": [{ "time": "2026-07-30T00:00:00Z", "health": "Healthy", "condition": 0 }] }
```
{/* ── /v1/providers ────────────────────────────────────────────────────── */}
 
<ApiPlayground
  method="GET"
  path="/v1/providers"
  description="List storage providers."
  params={[
    { name: "network", type: "select", required: true, default: "shelbynet", options: ["shelbynet", "testnet", "mainnet"] },
    { name: "status", type: "select", default: "active", options: ["active", "waitlisted", "all"] },
    { name: "az", type: "text", placeholder: "e.g. dc_australia" },
    { name: "limit", type: "number", default: "50" },
    { name: "offset", type: "number", default: "0" },
  ]}
/>
 
{/* ── /v1/providers/leaderboard ────────────────────────────────────────── */}
{/* sort options verified against api/src/routes/leaderboard.ts VALID_SORTS
   (2026-08-22): score | uptime | stake. "reputation" is not a real value —
   removed from both this block and the prose table above. */}
 
<ApiPlayground
  method="GET"
  path="/v1/providers/leaderboard"
  description="Ranked SP leaderboard."
  params={[
    { name: "network", type: "select", required: true, default: "shelbynet", options: ["shelbynet", "testnet", "mainnet"] },
    { name: "sort", type: "select", default: "score", options: ["score", "uptime", "stake"] },
    { name: "limit", type: "number", default: "20" },
  ]}
/>
 
{/* ── /v1/providers/{address} ─────────────────────────────────────────── */}
 
<ApiPlayground
  method="GET"
  path="/v1/providers/{address}"
  description="Single provider detail. Use a real address from the leaderboard response above — post-wipe shelbynet addresses differ from any older testnet examples."
  pathParams={[
    { name: "address", placeholder: "0x1e17b3abf249e2f5d0a4b89efb5a7ad7bb9727f226668440d8fcafbb3ee8d0d4" },
  ]}
  params={[
    { name: "network", type: "select", required: true, default: "shelbynet", options: ["shelbynet", "testnet", "mainnet"] },
  ]}
/>
 
{/* ── /v1/providers/{address}/history ─────────────────────────────────── */}
 
<ApiPlayground
  method="GET"
  path="/v1/providers/{address}/history"
  description="Health/condition history for a single provider over time."
  pathParams={[
    { name: "address", placeholder: "0x1e17b3abf249e2f5d0a4b89efb5a7ad7bb9727f226668440d8fcafbb3ee8d0d4" },
  ]}
  params={[
    { name: "network", type: "select", required: true, default: "shelbynet", options: ["shelbynet", "testnet", "mainnet"] },
    { name: "from", type: "text", placeholder: "ISO timestamp, optional" },
    { name: "to", type: "text", placeholder: "ISO timestamp, optional" },
    { name: "resolution", type: "select", default: "1h", options: ["5m", "1h"] },
  ]}
/>